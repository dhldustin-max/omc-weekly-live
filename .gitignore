# Node / npm
node_modules/
package-lock.json

# Scraper auth + secrets (one session file per POS)
clover-session.json
verona-session.json
toast-session.json
.env
.env.local

# Playwright artifacts
test-results/
playwright-report/
playwright/.cache/

# Scraper output / temp
/tmp/
*.local.json

# OS
.DS_Store

# Per-week retry counter written by scripts/weekly-update.js (local only)
.weekly-attempts.json

# Finder duplicate-file artefacts
* 2
*\ 2.*
